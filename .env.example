# Database Connection with SSL
# Replace username, password with your actual PostgreSQL credentials
# The sslmode=require ensures SSL connection is used
DATABASE_URL="postgresql://gtc_user:gtc_password@localhost:5432/gtc_db"
DIRECT_URL="postgresql://username:password@34.70.114.57:6432/gtc?schema=public&sslmode=require"
DATABASE_URL="postgresql://postgres:Forhopp_2024$@10.11.128.5:6432/gtc"

# Alternative SSL modes if 'require' doesn't work:
# sslmode=prefer (tries SSL, falls back to non-SSL)
# sslmode=verify-ca (verifies certificate)
# sslmode=verify-full (verifies certificate and hostname)

# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
